import React from 'react';
import {View} from 'react-native';

export const MenuProvider = (children: React.JSX.Element) => (
  <View>{children}</View>
);
export const Menu = (children: React.JSX.Element) => <View>{children}</View>;
export const MenuTrigger = (children: React.JSX.Element) => (
  <View>{children}</View>
);
export const MenuOptions = (children: React.JSX.Element) => (
  <View>{children}</View>
);
export const MenuOption = (children: React.JSX.Element) => (
  <View>{children}</View>
);
